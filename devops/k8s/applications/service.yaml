apiVersion: v1
kind: Service
metadata:
  name: model-router-service
  namespace: model-router
  labels:
    app: model-router
spec:
  selector:
    app: model-router
  ports:
  - name: http
    port: 80
    targetPort: 8000
    protocol: TCP
  type: ClusterIP